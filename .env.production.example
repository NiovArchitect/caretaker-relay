# Caretaker Relay UI — production / online evaluator build
# Set at build time on Render (Vite bakes these into the static bundle).

VITE_CARE_TRANSPORT=http
# Public HTTPS Care API origin (example):
# VITE_CARE_API_URL=https://caretaker-relay-care-api.onrender.com
VITE_CARE_API_URL=
VITE_CARE_MODE=fixture
