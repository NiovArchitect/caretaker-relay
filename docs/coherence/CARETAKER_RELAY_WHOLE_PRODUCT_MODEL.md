# Whole Product Model

AuthN (Foundation Entity / lab JWT) → AuthZ (authorize + membership + min-necessary) → Surfaces (UI) → Relay (pre-retrieval) → Audit (CARE_DATA_VIEW + consequential) → Revocation (shared store) → Suspension (account status).

Modes: synthetic_demo | consumer | regulated_restricted | regulated_ai_enabled | test
