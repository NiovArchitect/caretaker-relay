# React / Main Thread Audit
- **P0:** Hooks after conditional return → React #310
- Polling: App 5s + Today 4s + Relay coord 2s → reduced
- Large notification lists: already capped/filtered in prior pass
