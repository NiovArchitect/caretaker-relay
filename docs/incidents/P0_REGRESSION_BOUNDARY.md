# Regression Boundary
- Crash introduced when `useEffect(cr-navigate)` was added **after** `if (!session) return` in judge aha pass (f02cfa2 era / retained in 270d0fa)
- Design glass from b2c7c90+ contributes scroll compositor risk, not the total blank after login
- Primary blank = React #310
