# Notification Quality Contract
1. Only active care recipient
2. Hide resolved/seen by default
3. Suppress known lab/test markers (JL-SMOKE, torture markers) from primary inbox
4. Cap visible items with expand control
5. Every item shows why + source + time + action
6. Mark all read available
